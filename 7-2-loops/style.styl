columns = 12

div
  box-sizing border-box
  background-color orange
  height 40px
  margin-top 10px
  float left
  border 1px solid #000

for i in (columns..1)
  .col-{i}
    width: 'calc(100% / %s)' % (columns/i);
